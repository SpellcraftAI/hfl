task: text-generation
checkpoint: distilgpt2
inference:
  sequence: Today is such a
tokenizer:
  checkpoint: bert-base-uncased
  padding: true
  truncation: true
model:
  checkpoint: bert-base-uncased
dataset:
  name: glue
  set: mrpc
  split: test