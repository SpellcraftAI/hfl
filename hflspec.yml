mode: train
task: sequence-classification
checkpoint: bert-base-uncased
inference:
  sequence: Apples are good.
tokenizer:
  padding: true
  truncation: true
model:
  num_labels: 2
dataset:
  name: glue
  set: mrpc
